<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="report_saleorder_document" inherit_id="sale.report_saleorder_document">
        <xpath expr="//td[@name='td_name']" position="inside">
            <t t-if="line.cpq_configuration_summary">
                <strong>Configuration:</strong>
                <br/>
                <div style="margin-top: 4px; line-height: 1.4; font-size: 85%; color: #555; border-top: 1px solid #ccc; padding-top: 4px;">
                    <t t-out="line.cpq_configuration_summary"/>
                </div>
            </t>
        </xpath>
        <xpath expr="//td[@name='td_priceunit']" position="replace">
            <td class="text-end" name="td_priceunit">
                <t t-if="line.product_template_id.cpq_ok and not line.cpq_configuration_json">
                    <span class="text-danger">Not configured</span>
                </t>
                <t t-elif="line.product_template_id.cpq_ok and line.price_unit == 0.0">
                    <span class="text-muted">Configure to see price</span>
                </t>
                <t t-else="">
                    <span t-field="line.price_unit" t-options="{'widget': 'monetary', 'display_currency': doc.currency_id}"/>
                </t>
            </td>
        </xpath>
    </template>
    <template id="sale_discount_display_amount_document" inherit_id="sale.report_saleorder_document">
        <xpath expr="//div[@id='total']//table" position="inside">
            <t groups="sale.group_discount_per_so_line" t-if="doc.discount_total">
                <t t-set="is_discount" t-value="doc.company_id.report_total_without_discount and doc.price_total_no_discount != doc.amount_total" />
                <tr class="border-black" t-if="is_discount">
                    <td name="td_total_without_discount_label">
                        <strong>Total Without Discount</strong>
                    </td>
                    <td name="td_total_without_discount" class="text-end">
                        <span t-field="doc.price_total_no_discount" />
                    </td>
                </tr>

                <tr t-attf-class="{{ 'border-black' if not is_discount else '' }}">
                    <td name="td_discount_total_label">
                        <strong>Discount</strong>
                    </td>
                    <td name="td_discount_total" class="text-end">
                        <span t-field="doc.discount_total" />
                    </td>
                </tr>
            </t>
        </xpath>
    </template>
</odoo>
