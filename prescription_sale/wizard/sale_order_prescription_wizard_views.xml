<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="sale_order_create_prescription_action" model="ir.actions.act_window">
        <field name="name">Create Prescription</field>
        <field name="res_model">sale.order.prescription.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
    <record id="sale_order_prescription_wizard_form_view" model="ir.ui.view">
        <field name="name">sale.order.prescription.wizard.form</field>
        <field name="model">sale.order.prescription.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="line_ids" nolabel="1" colspan="2">
                        <tree editable="bottom">
                            <field name="order_id" invisible="1" />
                            <field name="allowed_product_ids" invisible="1" />
                            <field name="product_id" options="{'no_create': True}" />
                            <field name="quantity" />
                            <field name="product_uom_category_id" invisible="1" />
                            <field
                                name="uom_id"
                                groups="uom.group_uom"
                                options="{'no_create': True}"
                            />
                            <field name="allowed_picking_ids" invisible="1" />
                            <field
                                name="picking_id"
                                options="{'no_create': True}"
                                required="1"
                            />
                            <field name="operation_id" />
                        </tree>
                    </field>
                </group>
                <group>
                    <field name="commercial_partner_id" invisible="1" />
                    <field
                        name="partner_shipping_id"
                        domain="[('id', 'child_of', commercial_partner_id)]"
                    />
                    <field
                        name="location_id"
                        options="{'no_create': True, 'no_open': True}"
                        groups="stock.group_stock_multi_locations"
                        required="1"
                    />
                </group>
                <footer>
                    <button
                        name="create_and_open_prescription"
                        string="Accept"
                        type="object"
                        class="btn-primary"
                    />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>
</odoo>
