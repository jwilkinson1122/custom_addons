<odoo>
  <data>
    <!-- Prescription Tree View -->
    <record model="ir.ui.view" id="custom_orthotics_prescription_tree">
      <field name="name">custom_orthotics.prescription_tree</field>
      <field name="model">custom_orthotics.prescription</field>
      <field name="arch" type="xml">
        <tree decoration-info="state == 'draft'" decoration-muted="state == 'canceled'" decoration-primary="state == 'hold'" decoration-success="state == 'confirmed'">
          <field name="prescription_id" />
          <field name="owner" />
          <field name="patient" />
          <field name="practice" />
          <field name="date" />
          <field name="state" />
        </tree>
      </field>
    </record>

    <!-- Prescription Form View -->
    <record model="ir.ui.view" id="custom_orthotics_prescription_form">
      <field name="name">custom_orthotics.prescription_form</field>
      <field name="model">custom_orthotics.prescription</field>
      <field name="arch" type="xml">
        <form string="Patient">
          <header>
            <button name="%(custom_orthotics.create_order_wizard)d" string="Create Order" type="action" class="oe_highlight" context="{'default_prescription':active_id,'default_owner':owner,'default_patient':patient,'default_practice':practice,'default_date':date}" attrs="{'invisible': [('state','!=', 'confirmed')]}" />
            <button name="action_confirm" type="object" string="Confirm" class="oe_highlight" states="draft" />
            <button name="action_cancel" type="object" string="Cancel" />
            <button name="action_hold" type="object" string="Hold" />
            <field name="state" widget="statusbar" statusbar_visible="draft,confirmed" />
          </header>
          <sheet>
            <div class="oe_title">
              <h1>
                <field name="prescription_id" />
              </h1>
            </div>
            <group name="data">
              <group>
                <field name="practice" />
                <field name="owner" string="Doctor"/>
              </group>
              <group>
                <field name="patient" />
                <field name="date" />
              </group>
              <group>
                <field name="item_product" />
              </group>

            </group>
            <group>
              <field name="foot_selection" widget="radio" options="{'horizontal': true}"/>
            </group>
            <group name="Accommodations">
              <field name="item_service" />
            </group>
            <!-- <group name="Accommodations">
              <field name="item_service" />
            </group> -->
            <!-- <group string="Description">
              <field name="description" nolabel="1" placeholder="This is Prescription Note" />
            </group> -->

          </sheet>
          <div class="oe_chatter">
            <field name="message_follower_ids" widget="mail_followers" />
            <field name="activity_ids" widget="mail_activity" />
            <field name="message_ids" widget="mail_thread" options="{'post_refresh': 'recipients'}" />
          </div>
        </form>
      </field>
    </record>

    <!-- Prescription Kanban View -->
    <record id="custom_orthotics_prescription_kanban" model="ir.ui.view">
      <field name="name">custom_orthotics.prescription_kanban</field>
      <field name="model">custom_orthotics.prescription</field>
      <field name="arch" type="xml">
        <kanban default_group_by="state" class="o_kanban_small_column">
          <field name="id" />
          <field name="prescription_id" />
          <field name="practice_name" />
          <field name="patient" />
          <field name="date" />
          <templates>
            <t t-name="kanban-box">
              <div t-attf-class="oe_kanban_global_click">
                <div class="row mb4">
                  <div class="col-6">
                    <strong>
                      <span>
                        <field name="prescription_id" />
                      </span>
                    </strong>
                  </div>
                  <div class="col-6 text-right">
                    <strong>
                      <i class="fa fa-calendar" role="img" aria-label="Date" title="Date" />
                      <field name="date" />
                    </strong>
                  </div>
                </div>
                <div class="row">
                  <div class="col-12">
                    <span>
                      <field name="patient" />
                    </span>
                  </div>
                </div>
                <div class="row">
                  <div class="col-6">
                    <span>
                      Practice
                      <field name="practice_name" />
                    </span>
                  </div>
                  <div class="col-6">
                    <span class="float-right" style="font-size:16px;">
                      <field name="state" widget="label_selection" options="{'classes': {'draft': 'info', 'confirmed': 'success', 'canceled': 'danger'}}" />
                    </span>
                  </div>
                </div>
              </div>
            </t>
          </templates>
        </kanban>
      </field>
    </record>

    <!-- Prescription Calendar View-->
    <record id="custom_orthotics_prescription_calendar" model="ir.ui.view">
      <field name="name">custom_orthotics.prescription_calendar</field>
      <field name="model">custom_orthotics.prescription</field>
      <field name="arch" type="xml">
        <calendar string="Prescription Calendar" date_start="date" color="patient" mode="month" quick_add="False">
          <field name="prescription_id" />
          <field name="patient" />
          <field name="practice" />
        </calendar>
      </field>
    </record>


    <!-- Prescription Graph View -->
    <record id="custom_orthotics_prescription_graph" model="ir.ui.view">
      <field name="name">custom_orthotics.patient_graph</field>
      <field name="model">custom_orthotics.prescription</field>
      <field name="arch" type="xml">
        <graph string="Prescription Graph" type="pie">
          <field name="date" />
          <field name="date" type="measure" />
        </graph>
      </field>
    </record>

    <!-- Prescription Search View -->
    <record id="custom_orthotics_prescription_search" model="ir.ui.view">
      <field name="name">custom_orthotics.prescription_search</field>
      <field name="model">custom_orthotics.prescription</field>
      <field name="arch" type="xml">
        <search string="Search Prescription">
          <field name="prescription_id" filter_domain="[('prescription_id', 'ilike', self)]" />
          <field name="practice_name" />
          <field name="date" />
          <separator />
          <filter string="Today" name="date_today" domain="[('date', '&gt;=', datetime.datetime.now().strftime('%Y-%m-%d 00:00:00')),('date', '&lt;=',datetime.datetime.now().strftime('%Y-%m-%d 23:23:59'))]" />
          <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]" />
          <filter string="Confirmed" name="confirmed" domain="[('state', '=', 'confirmed')]" />
          <filter string="Canceled" name="canceled" domain="[('state', '=', 'canceled')]" />
          <group expand="0" string="Group By">
            <filter string="Status" name="state" context="{'group_by':'state'}" />
            <filter string="Practice" name="practice_name" context="{'group_by':'practice_name'}" />
          </group>
        </search>
      </field>
    </record>

    <!-- Prescription Action Window -->
    <record model="ir.actions.act_window" id="custom_orthotics_prescription_action_window">
      <field name="name">Prescription</field>
      <field name="res_model">custom_orthotics.prescription</field>
      <field name="view_mode">kanban,tree,form,calendar,graph</field>
      <field name="context">{'search_default_date_today': 1}</field>
    </record>

    <!-- Menu Root -->
    <menuitem name="Patient Clinic" id="custom_orthotics.root_menu" web_icon="custom_orthotics,static/description/icon.png" />

    <!-- Menu Prescription -->
    <menuitem name="Prescription" id="custom_orthotics.prescription" parent="custom_orthotics.root_menu" action="custom_orthotics_prescription_action_window" />
  </data>
</odoo>