<odoo>
  <data>

    <!-- Patient Tree View -->
    <record model="ir.ui.view" id="custom_orthotics_patient_tree">
      <field name="name">custom_orthotics.patient_tree</field>
      <field name="model">custom_orthotics.patient</field>
      <field name="arch" type="xml">
        <tree string="Patient">
          <field name="patient_id" />
          <field name="name" />
          <field name="owner_name" />
          <field name="patient_type" />
        </tree>
      </field>
    </record>

    <!-- Patient Form View -->
    <record model="ir.ui.view" id="custom_orthotics_patient_form">
      <field name="name">custom_orthotics.patient_form</field>
      <field name="model">custom_orthotics.patient</field>
      <field name="arch" type="xml">
        <form string="Patient">
          <sheet>
            <!-- Patient Button Box -->
            <div class="oe_button_box" name="button_box">
              <!-- Patient Prescription Button -->
              <button name="open_patient_prescription" class="oe_stat_button" type="object" icon="fa-calendar">
                <field string="Janji" name="prescription_count" widget="statinfo" />
              </button>
            </div>
            <div class="oe_title">
              <h1>
                <field name="patient_id" />
              </h1>
            </div>
            <group>
              <field name="image" widget="image" class="oe_avatar" />
            </group>
            <group name="data">
              <group>
                <field name="name" />
                <field name="age" />
                <field name="gender" />
              </group>
              <group>
                <field name="owner" string="Doctor"/>
                <field name="patient_type" />
                <field name="patient_breed" />
              </group>
            </group>
            <group string="Description">
              <field name="description" nolabel="1" placeholder="This is Animal Note" />
            </group>
          </sheet>
          <div class="oe_chatter">
            <field name="message_follower_ids" widget="mail_followers" />
            <field name="activity_ids" widget="mail_activity" />
            <field name="message_ids" widget="mail_thread" options="{'post_refresh': 'recipients'}" />
          </div>
        </form>
      </field>
    </record>

    <!-- Patient Kanban View -->
    <record id="custom_orthotics_patient_kanban" model="ir.ui.view">
      <field name="name">custom_orthotics.patient_kanban</field>
      <field name="model">custom_orthotics.patient</field>
      <field name="arch" type="xml">
        <kanban>
          <field name="id" />
          <field name="image" />
          <field name="name" />
          <field name="age" />
          <field name="owner_name" />
          <field name="patient_type" />
          <field name="patient_breed" />
          <templates>
            <t t-name="kanban-box">
              <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_custom_orthotics_patient_kanban">
                <div class="o_kanban_image">
                  <img t-att-src="kanban_image('item.template', 'image', record.id.raw_value)" alt="Item" />
                </div>
                <div class="oe_kanban_details">
                  <strong class="o_kanban_record_title">
                    <field name="name" />
                  </strong>
                  <div class="o_kanban_tags_section" />
                  <ul>
                    <li>
                      Doctor :
                      <field name="owner_name" />
                    </li>
                    <li>
                      Age :
                      <field name="age" />
                    </li>
                    <li>
                      Type :
                      <span>
                        <field name="patient_type" />
                        -
                        <field name="patient_breed" />
                      </span>
                    </li>
                  </ul>
                </div>
              </div>
            </t>
          </templates>
        </kanban>
      </field>
    </record>

    <!-- Patient Graph View -->
    <record id="custom_orthotics_patient_graph" model="ir.ui.view">
      <field name="name">custom_orthotics.patient_graph</field>
      <field name="model">custom_orthotics.patient</field>
      <field name="arch" type="xml">
        <graph string="Patient Graph" type="pie">
          <field name="patient_type" />
        </graph>
      </field>
    </record>

    <!-- Patient Search View -->
    <record id="custom_orthotics_patient_search" model="ir.ui.view">
      <field name="name">custom_orthotics.patient_search</field>
      <field name="model">custom_orthotics.patient</field>
      <field name="arch" type="xml">
        <search string="Search Patient">
          <field name="name" filter_domain="['|',('name', 'ilike', self)]" />
          <field name="patient_type" />
          <separator />
          <filter string="Male" name="male" domain="[('gender', '=', 'male')]" />
          <filter string="Female" name="female" domain="[('gender', '=', 'female')]" />
          <group expand="0" string="Group By">
            <filter string="Gender" name="gender" context="{'group_by':'gender'}" />
            <filter string="Type" name="patient_type" context="{'group_by':'patient_type'}" />
            <filter string="Breed" name="patient_breed" context="{'group_by':'patient_breed'}" />
          </group>
        </search>
      </field>
    </record>

    <!-- Patient Action Window -->
    <record model="ir.actions.act_window" id="custom_orthotics_patient_action_window">
      <field name="name">Patient</field>
      <field name="res_model">custom_orthotics.patient</field>
      <field name="view_mode">kanban,tree,form,graph</field>
    </record>

    <!-- Menu Root -->
    <menuitem name="Patient Clinic" id="custom_orthotics.root_menu" web_icon="custom_orthotics,static/description/icon.png" />

    <!-- Menu Patient -->
    <!-- <menuitem name="Patient" id="custom_orthotics.patient" parent="custom_orthotics.root_menu" /> -->

    <!-- Menu Patient -> Patient -->
    <menuitem name="Patient" id="custom_orthotics.menu_patient" parent="custom_orthotics.root_menu" sequence="3" action="custom_orthotics_patient_action_window" />
  </data>
</odoo>