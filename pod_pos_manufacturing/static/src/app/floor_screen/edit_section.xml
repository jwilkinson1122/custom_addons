<?xml version="1.0" encoding="UTF-8" ?>
<templates id="template" xml:space="preserve">

    <t t-name="pod_pos_manufacturing.EditSection">
        <div class="section selected position-absolute d-flex flex-column align-items-center justify-content-between overflow-hidden" 
            t-ref="root">
            <span class="label drag-handle d-flex align-items-center flex-grow-1 d fw-bolder fs-2">
                <t t-esc="props.section.name" />
            </span>
            <t t-if="pos.floorPlanStyle != 'kanban'" t-foreach="handles" t-as="handle" t-key="handle">
                <span class="section-handle" t-ref="{{handle}}" t-att-style="computeHandleStyle(handle)"/>
            </t>
        </div>
    </t>

</templates>
