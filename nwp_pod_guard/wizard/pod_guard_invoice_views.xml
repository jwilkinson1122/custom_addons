<?xml version="1.0" ?>

<odoo>
    <record id="pod_guard_invoice_form" model="ir.ui.view">
        <field name="name">pod.guard.invoice.form</field>
        <field name="model">pod.guard.invoice</field>
        <field name="arch" type="xml">
            <form string="Invoice guards">
                <group>
                    <group>
                        <field name="date_from" />
                    </group>
                    <group>
                        <field name="date_to" />
                    </group>
                </group>
                <footer>
                    <button
                        name="run"
                        string="Create"
                        type="object"
                        class="oe_highlight"
                    />
                    or
                    <button
                        name="action_cancel"
                        string="Cancel"
                        class="oe_link"
                        special="cancel"
                    />
                </footer>
            </form>
        </field>
    </record>
    <record id="pod_guard_invoice_action" model="ir.actions.act_window">
        <field name="name">Invoice guards</field>
        <field name="res_model">pod.guard.invoice</field>
        <field name="view_mode">form</field>

        <field name="target">new</field>
    </record>
    <menuitem
        id="pod_guard_invoice_menu"
        parent="nwp_pod_guard.pod_guard_root"
        groups="nwp_pod_guard.group_pod_guard_manager"
        action="pod_guard_invoice_action"
        sequence="40"
    />
</odoo>
