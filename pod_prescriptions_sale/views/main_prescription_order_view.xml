<?xml version="1.0"?>
<odoo>
    <data>

<!-- FORM VIEW  -->

        <record id="prescription_order_view" model="ir.ui.view">
            <field name="name">prescriptions.order.form</field>
            <field name="model">prescriptions.order</field>
            <field name="inherit_id" ref="pod_prescriptions.view_order_form"/>
            <field name="arch" type="xml">
                <header>
                        <button name="%(action_transientmodel_wizard)d" string="Create Sale Order" type="action" class="btn-primary" invisible="state in ('sales', 'cancel')"/>
                </header>
                   
            </field>
        </record>

    </data>
</odoo>