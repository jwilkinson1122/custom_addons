<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view -->
    <record id="pod_rx_order_line_tree_view" model="ir.ui.view">
        <field name="name">pod.rx.order.line.tree.view</field>
        <field name="model">pod.rx.order.line</field>
        <field name="priority">2</field>
        <field name="arch" type="xml">
            <tree string="Rx Order lines">
                <field name="prnt" />
                <field name="treatment_id" />
                <field name="form" />
                <field name="quantity" />
                <field name="short_comment" />
            </tree>
        </field>
    </record>

    <record id="pod_rx_order_line_form_view" model="ir.ui.view">
        <field name="name">pod.rx.order.line.form.view</field>
        <field name="model">pod.rx.order.line</field>
        <field name="arch" type="xml">
            <form string="Rx Order">
                <!-- <group colspan="4" col="4">
                    <field name="treatment_id" required="1" />
                    <field name="prnt" />
                    <field name="form" />
                </group>
                <group colspan="4" col="4">
                    <field name="quantity" />
                    <field name="short_comment" />
                </group> -->
                <group>
                    <group string="Right Foot" name="right_foot_details">
                        <field name="treatment_id" />
                        <!-- <field name="partner_id" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="document_type" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="document_number" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="partner_name" placeholder="Guest" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="out_service_description" placeholder="Out service description" attrs="{'invisible':[('reservation_type','not in',('out'))]}" />
                        <field name="email" placeholder="email" widget="email" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="mobile" placeholder="mobile" widget="phone" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="partner_internal_comment" placeholder="Comments about the customer" attrs="{'invisible':[('reservation_type','in',('out'))]}" />
                        <field name="closure_reason_id" placeholder="Closure reason" attrs="{'invisible':[('reservation_type','not in',('out'))]}" /> -->
                    </group>
                    <group string="Left Foot" name="left_foot_details">
                        <field name="left_treatment_id" />
                        <!-- <field name="show_update_pricelist" invisible="1" />
                        <field name="pricelist_id" options="{'no_open':True,'no_create': True}" attrs="{'invisible': [('reservation_type', '!=', 'normal')]}" />
                    
                        <field name="cancelled_reason" attrs="{'invisible': [('state', 'not in', ('cancel'))]}" />
                        <field name="adults" attrs="{'invisible': [('reservation_type','in',('out'))]}" />
                        <field name="children" attrs="{'invisible': [('reservation_type','in',('out'))]}" />
                        <field name="children_occupying" attrs="{'invisible': [('children', '=', 0)]}" />
                        <field name="allowed_room_ids" invisible="1" />
                        <field name="folio_payment_state" invisible="1" />
                
                        <field name="board_service_room_id" domain="[('pms_room_type_id', '=', room_type_id)]" options="{'no_create': True,'no_open': True}" attrs="{'invisible': [('reservation_type','in',('out'))]}" />
                        <field name="agency_id" attrs="{'invisible': [('reservation_type', '!=', 'normal')]}" />
                        <field name="external_reference" attrs="{'invisible': [('agency_id', '=', False)]}" />
                        <field name="channel_type_id" attrs="{'readonly':[('agency_id','!=', False)], 'invisible': [('reservation_type', '!=', 'normal')]}" />
                        <field placeholder="Arrival Hour" name="arrival_hour" />
                        <field placeholder="Departure Hour" name="departure_hour" /> -->
                    </group>
                    <div class="oe_clear" />
                </group>
            </form>
        </field>
    </record>

</odoo>
