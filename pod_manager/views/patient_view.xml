<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="patient_tree" model="ir.ui.view">
        <field name="name">pod.manager.patient.tree</field>
        <field name="model">pod.manager.patient</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="address"/>
                <field name="gender"/>
                <field name="phone"/>
                <field name="email"/>
                <field name="age"/>
                <field name="patient_prescription_ids"/>
            </tree>
        </field>
    </record>

    <record id="patient_form" model="ir.ui.view">
        <field name="name">pod.manager.patient.form</field>
        <field name="model">pod.manager.patient</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_url" string="Open github" class="btn-primary" type="object"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="address"/>
                            <field name="phone"/>
                        </group>
                        <group>
                            <field name="gender" invisible="context.get('hide_gender')"/>
                            <field name="email"/>
                            <field name="age"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Prescriptions">
                            <group string="No. of Prescriptions:">
                                <h2>
                                    <field name="total_prescriptions"/>
                                </h2>
                            </group>
                            <field name="patient_prescription_ids"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="patient_search" model="ir.ui.view">
        <field name="name">pod.manager.patient.search</field>
        <field name="model">pod.manager.patient</field>
        <field name="arch" type="xml">
            <search string="Patients">
                <field name="name"/>
                <field name="gender"/>
                <field name="phone"/>
                <field name="age"/>
            </search>
        </field>
    </record>

    <record id="patient_kanban" model="ir.ui.view">
        <field name="name">pod.manager.patient.kanban</field>
        <field name="model">pod.manager.patient</field>
        <field name="arch" type="xml">
            <kanban default_group_by="gender">
                <field name="name"/>
                <field name="gender"/>
                <field name="phone"/>
                <field name="email"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <h1 class="o_kanban_record_title">
                                    <field name="name"/>
                                </h1>
                                <ul>
                                    <li>Gender :
                                        <field name="gender"/>
                                    </li>
                                    <li>Phone :
                                        <field name="phone"/>
                                    </li>
                                    <li>Email :
                                        <field name="email"/>
                                    </li>
                                    <li>Age :
                                        <field name="age"/>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="all_patient_action" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pod.manager.patient</field>
        <field name="view_mode">tree,form,search,kanban</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Patients List
            </p>
        </field>
    </record>

    <!-- <record id="male_patient_action" model="ir.actions.act_window">
        <field name="name">Male Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pod.manager.patient</field>
        <field name="view_mode">tree,form,search,kanban</field>
        <field name="context">{'default_gender' : 'male', 'hide_gender' : 1}</field>
        <field name="domain">[('gender','=', 'male')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Patients List
            </p>
        </field>
    </record> -->

    <!-- <record id="female_patient_action" model="ir.actions.act_window">
        <field name="name">Female Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pod.manager.patient</field>
        <field name="view_mode">tree,form,search,kanban</field>
        <field name="context">{'default_gender' : 'female', 'hide_gender' : 1}</field>
        <field name="domain">[('gender','=', 'female')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Patients List
            </p>
        </field>
    </record> -->

    <!-- action server for creating prescription from patient -->
    <record id="action_create_prescription" model="ir.actions.server">
        <field name="name">Create prescription</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="pod_manager.model_pod_manager_patient"/>
        <field name="binding_model_id" ref="pod_manager.model_pod_manager_patient"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
            if record:
                actions_values = env.ref('pod_manager.prescription_action').sudo().read()[0]
                actions_values.update({'context': env.context})
                action = actions_values
        </field>
    </record>

    <!--  action sale practice device  -->
    <record id='action_product' model='ir.actions.act_window'>
        <field name='name'>Products</field>
        <field name='res_model'>product.template</field>
        <field name='view_mode'>tree,form</field>
    </record>

    <!--  action sales order  -->
    <record id='action_sales' model='ir.actions.act_window'>
        <field name='name'>Orders</field>
        <field name='res_model'>sale.order</field>
        <field name='view_mode'>tree,form</field>
    </record>

    <menuitem id="practice_root" name="Podiatry Practice" web_icon="pod_manager,static/description/icon.png"/>

    <menuitem id="practice_patient" name="Patients" parent="practice_root" groups="pod_manager.group_practice_admin" action="all_patient_action" sequence='6'/>

    <!-- <menuitem id="practice_patient_all" name="All Patients" parent="practice_patient" action="all_patient_action" sequence='6'/> -->

    <!-- <menuitem id="practice_patient_male" name="Male Patients" parent="practice_patient" action="male_patient_action" sequence='7'/>

    <menuitem id="practice_patient_female" name="Female Patients" parent="practice_patient" action="female_patient_action" sequence='8'/> -->

    <menuitem id='menu_sales' action='action_sales' name='Orders' parent='practice_root' sequence='10'/>

    <menuitem id='menu_product' action='action_product' name='Products' parent='practice_root' sequence='11'/>


</odoo>