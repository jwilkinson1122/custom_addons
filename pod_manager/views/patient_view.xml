<?xml version="1.0" encoding="utf-8" ?>

<odoo>

    <record id="patient_tree" model="ir.ui.view">
        <field name="name">podiatry.patient.tree</field>
        <field name="model">podiatry.patient</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" string="Patient Name"/>
                <field name="surname" string="Patient Surname"/>
                <field name="gender"/>
            </tree>
        </field>
    </record>

    <record id="patient_form" model="ir.ui.view">
        <field name="name">podiatry.patient.form</field>
        <field name="model">podiatry.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <field name="image" widget="image" class="oe_avatar"/>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="surname"/>
                            <field name="gender"/>
                            <field name="birth"/>
                        </group>
                        <group>
                            <field name="active" invisible="1"/>
                            <field name="tc_no"/>
                        </group>
                    </group>
                </sheet>
                <!-- Chatter -->
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- <record model="ir.actions.act_window" id="patient_action">
        <field name="name">Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">podiatry.patient</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                create your first patient!
            </p>
        </field>
    </record> -->

    <!-- <menuitem id="podiatry_patient" name="Patients" parent="podiatry_conf_root" action="patient_action" sequence="10"/> -->



</odoo>
