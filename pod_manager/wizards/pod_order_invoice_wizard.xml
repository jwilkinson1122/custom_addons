<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="pod_order_invoice_wizard_view" model="ir.ui.view">
            <field name="name">pod.order.invoice.wizard.view</field>
            <field name="model">pod.order.invoice</field>
            <field name="arch" type="xml">
                <form string="Create pod_order invoices">
                    <separator
                        string="Do you want to create the pod_order invoice(s) ?" />
                    <newline />
                    <separator string="                  "
                        colspan="6" />
                    <footer>
                        <button name="pod_order_invoice"
                            class="oe_highlight" string="Create Order Invoices"
                            type="object" />
                        or
                        <button special="cancel" string="Cancel"
                            class="oe_highlight" />
                    </footer>
                </form>
            </field>
        </record>

        <act_window id="action_pod_order_invoice1"
            name="Create Order Invoice" res_model="pod.order.invoice"
            binding_model="pod.order" view_mode="form"
            target="new" />

</odoo> -->
