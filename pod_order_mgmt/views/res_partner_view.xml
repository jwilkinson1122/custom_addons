<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- <record id="view_res_partner_form_inherit_pod" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.pod</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="groups_id" eval="[(4, ref('pod_base.group_pod_user'))]"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button name="open_partner_prescriptions" class="oe_stat_button" type="object" icon="fa-file-prescription">
                    <field string="Prescription" name="prescription_count" widget="statinfo"/>
                </button>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <field name="patient_ids" invisible="1"/>
                <button type="action" name="%(action_pod_patients_from_partner)d" class="oe_stat_button" icon="fa-user" attrs="{'invisible' : [('patient_ids', '=', [])]}">
                    <field name="patient_count" string="Patients" widget="statinfo"/>
                </button>
            </xpath>
        </field>
    </record> -->

    <!-- Search -->
    <record id="view_res_partner_search_inherit_pod" model="ir.ui.view">
        <field name="name">res.partner.search.inherit.pod</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="groups_id" eval="[(4, ref('pod_base.group_pod_user'))]"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='inactive']" position="before">
                <filter string="Patients" name="is_patient" domain="[('is_patient', '=', True)]" />
                <separator/>
            </xpath>
        </field>
    </record>

    <!-- Action > TODO: Add integration with the Contacts module -->
    <record id="action_res_partner_patients" model="ir.actions.act_window">
        <field name="name">Contacts</field>
        <field name="res_model">res.partner</field>
        <field name="context">{'search_default_is_patient': True}</field>
    </record>

    <!-- <menuitem id="menu_res_partners" name="Contacts" action="action_res_partner_patients" parent="pod_order_management.menu_configuration_root" sequence="20"/> -->

</odoo>
