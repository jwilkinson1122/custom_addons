<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="prescription_redelivery_wizard_view_form" model="ir.ui.view">
        <field name="name">prescription.delivery.wizard.form</field>
        <field name="model">prescription.delivery.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="scheduled_date" />
                        <field name="warehouse_id" invisible="type != 'replace'"/>
                        <field name="prescription_return_grouping" invisible="type != 'replace' or prescription_count == 1"/>
                    </group>
                    <group>
                        <field name="product_uom_category_id" invisible="1" />
                        <field name="product_id" invisible="type != 'replace' or prescription_count &gt; 1" required="type == 'replace' and prescription_count == 1"/>
                        <label for="product_uom_qty" invisible="prescription_count &gt; 1"/>
                        <div class="o_row" invisible="prescription_count &gt; 1">
                            <field name="product_uom_qty" required="prescription_count == 1"/>
                            <field name="product_uom" groups="uom.group_uom" required="prescription_count == 1" domain="[('category_id', '=', product_uom_category_id)]" />
                            <field name="product_uom" invisible="1" />
                        </div>
                    </group>
                </group>
                <field name="prescription_count" invisible="1" />
                <field name="type" invisible="1" />
                <footer>
                    <button name="action_deliver" string="Deliver" type="object" class="btn-primary" />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="prescription_delivery_wizard_action" model="ir.actions.act_window">
        <field name="name">Return to customer</field>
        <field name="res_model">prescription.delivery.wizard</field>
        <field name="view_mode">form</field>
        <field name="binding_model_id" ref="prescription.model_prescription" />
        <field name="binding_view_types">list</field>
        <field name="target">new</field>
        <field name="context">{'prescription_delivery_type': 'return'}</field>
    </record>
</odoo>
