<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="prescription_finalization_view_search" model="ir.ui.view">
        <field name="model">prescription.finalization</field>
        <field name="arch" type="xml">
            <search string="Prescription Finalization Reasons">
                <field name="name" />
                <filter string="Archived" name="inactive" domain="[('active','=',False)]" />
                <filter string="Active" name="active" domain="[('active','!=',False)]" />
            </search>
        </field>
    </record>
    <record id="view_prescription_finalization_form" model="ir.ui.view">
        <field name="name">Prescription Finalization Reasons</field>
        <field name="model">prescription.finalization</field>
        <field name="arch" type="xml">
            <form string="Prescription Finalization">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="bg-danger" invisible="active"/>
                    <group>
                        <field name="name" />
                        <field name="company_id" groups="base.group_multi_company" />
                        <field name="company_id" invisible="1" />
                        <field name="active" invisible="1" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="view_prescription_finalization_list" model="ir.ui.view">
        <field name="name">Prescription Finalization Reasons</field>
        <field name="model">prescription.finalization</field>
        <field eval="6" name="priority" />
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="company_id" groups="base.group_multi_company" />
                <field name="company_id" invisible="1" />
            </tree>
        </field>
    </record>
    <!-- <record id="action_prescription_finalization" model="ir.actions.act_window">
        <field name="name">Prescription Finalization Reasons</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">prescription.finalization</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            Create a new Prescription finalization
            </p>
            <p>
            Manage Prescription finalization reasons to better classify them for tracking and analysis purposes.
            </p>
        </field>
    </record> -->
    <!-- <menuitem id="prescription_configuration_prescription_finalization_menu" name="Prescription Finalization Reasons" parent="prescription_config_menu" action="action_prescription_finalization" groups="prescription.group_prescription_manual_finalization" /> -->
</odoo>
