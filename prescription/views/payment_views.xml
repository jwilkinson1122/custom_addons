<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">payment.provider.form.inherit.prescription</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="payment_form" position="inside">
                <field name="so_reference_type" invisible="code != 'custom'"/>
            </group>
        </field>
    </record>

    <record id="transaction_form_inherit_prescription" model="ir.ui.view">
        <field name="name">payment.transaction.form.inherit.prescription.payment</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button name="action_view_prescriptions" type="object"
                        class="oe_stat_button" icon="fa-money"
                        invisible="prescription_ids_nbr == 0">
                    <field name="prescription_ids_nbr" widget="statinfo" string="Prescription Order(s)"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
