<?xml version="1.0"?>
<odoo>
    <data>

        <!-- MAIN MENU -->
        <menuitem name="POD ERP" id="prescription_main_menu" sequence="125" groups="prescription.group_prescription_registration_desk" web_icon="prescription,static/description/icon.png"/>

        <!-- <record id="prescription.menu_accounts_root" model="ir.ui.menu">

        </record> -->

        <!-- <record id="prescription.menu_clinics" model="ir.ui.menu">
            <field name="action" ref="prescription.action_pod_clinics"/>
        </record> -->

        <!-- <record id="prescription.menu_contacts" model="ir.ui.menu">
            <field name="action" ref="prescription.action_pod_contacts"/>
        </record> -->

        <!-- <record id="prescription.menu_patients" model="ir.ui.menu">
            <field name="action" ref="prescription.action_pod_patients"/>
        </record> -->

        <!-- ACCOUNTS -->
        <menuitem name="Accounts" id="menu_accounts_root" parent="prescription.prescription_main_menu" sequence="1" groups="prescription.group_prescription_registration_desk"/>
        <menuitem name="Clinics" id="menu_clinics" parent="prescription.menu_accounts_root" sequence="1" groups="prescription.group_prescription_user"/>
        <menuitem name="Contacts" id="menu_contacts" parent="prescription.menu_accounts_root" sequence="2" groups="prescription.group_prescription_user"/>
        <menuitem name="Patients" id="menu_patients" parent="prescription.menu_accounts_root" sequence="3" groups="prescription.group_prescription_user"/>


        <!-- HEADER: PRESCRIPTIONS -->
        <menuitem name="Prescriptions" id="menu_prescription_prescription" sequence="2" parent="prescription.prescription_main_menu" groups="prescription.group_prescription_registration_desk"/>

        <!-- HEADER: REPORTING -->
        <menuitem name="Reporting" id="menu_reporting_prescriptions" sequence="50" parent="prescription_main_menu" groups="prescription.group_prescription_user"/>

        <!-- HEADER: CONFIGURATION -->
        <menuitem name="Configuration" id="menu_prescription_configuration" sequence="99" parent="prescription_main_menu" groups="prescription.group_prescription_user"/>
        <menuitem name="Prescription Templates" id="menu_prescription_type" sequence="1" parent="menu_prescription_configuration"/>
        <menuitem name="Prescription Stages" id="prescription_stage_menu" sequence="2" parent="menu_prescription_configuration"/>
        <menuitem name="Mail Schedulers" id="menu_prescription_mail_schedulers" sequence="10" parent="menu_prescription_configuration" groups="base.group_no_one"/>
        <menuitem name="Prescription Tags Categories" id="menu_prescription_category" sequence="3" parent="menu_prescription_configuration"/>

    </data>
</odoo>
