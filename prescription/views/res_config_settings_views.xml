<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit.hr.prescription</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="90"/>
            <field name="inherit_id" ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form" position="inside">
                    <app data-string="Prescription" id="prescription" string="Prescription" name="prescription" groups="prescription.prescription_group_manager">
                        <block title="Prescription Management" id="end_prescription_setting">
                            <setting string="End Date Prescription Alert">
                                <div class="text-muted content-group mt16">
                                    <span>Send an alert </span>
                                    <field name="delay_alert_prescription" class="text-center" style="width: 10%; min-width: 4rem;" />
                                    <span> days before the end date</span>
                                </div>
                            </setting>
                        </block>
                    </app>
                </xpath>
            </field>
        </record>

        <record id="prescription_config_settings_action" model="ir.actions.act_window">
            <field name="name">Settings</field>
            <field name="res_model">res.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">inline</field>
            <field name="context">{'module' : 'prescription', 'bin_size': False}</field>
        </record>

        <menuitem id="prescription_config_settings_menu" name="Settings"
            parent="prescription.prescription_configuration" sequence="0" action="prescription_config_settings_action"
            groups="base.group_system"/>
    </data>
</odoo>
