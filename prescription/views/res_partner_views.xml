<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <div name="button_box">
                <button name="action_view_prescription" type="object" class="oe_stat_button" icon="fa-reply" invisible="prescription_count == 0" groups="prescription_contacts.group_contacts_user">
                    <field name="prescription_count" widget="statinfo" string="Prescription" />
                </button>
            </div>
        </field>
    </record>
</odoo>
