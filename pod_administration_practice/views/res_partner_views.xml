<?xml version="1.0" encoding="utf-8" ?>

<odoo>
    <record id="pod_practice_location_action" model="ir.actions.act_window">
        <field name="name">Podiatry Locations</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="domain">[('is_location', '=', True), ('practice_id', '=', active_id)]
        </field>
        <field name="context">{
            'default_company_type':'company',
            'default_is_company':True, 
            'default_is_location': True,
            'default_is_pod': True,
            'default_practice_id': active_id,
            }
        </field>
    </record>
    <record id="res_partner_view_form" model="ir.ui.view">
        <field name="name">res.partner.view.form</field>
        <field name="model">res.partner</field>
        <field name="groups_id" eval="[(4, ref('pod_base.group_pod_user'))]" />
        <field name="inherit_id" ref="pod_administration_location.res_partner_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='child_ids']//form//field[@name='parent_id']" position="after">
                <field name="is_practice" invisible="1" />
            </xpath>
            <field name="is_pod" position="before">
                <field name="is_practice" />
            </field>
            <xpath expr="//field[@name='is_pod']/.." position="after">
                <group name="location_config" attrs="{'invisible': [('is_location','=',False)]}">
                    <field name="practice_id" attrs="{'required': [('is_location', '=', True)]}" />
                </group>
            </xpath>
            <div name="button_box" position="inside">
                <button type="action" name="%(pod_practice_location_action)d" class="oe_stat_button" icon="fa-h-square" attrs="{'invisible': [('is_practice', '=', False)]}">
                    <field name="location_count" widget="statinfo" string="Locations" />
                    <field name='location_ids' invisible="1" />
                </button>
            </div>
            <!-- <xpath expr='//page[@name="internal_notes"]' position="before">
                <page string="Locations" attrs="{'invisible': [('is_practice','=',False)]}">
                    <field name="location_ids" context="{'practice_id': active_id, 'default_is_location': True, 'default_type':'other'}" mode="kanban">
                        <kanban on_create="base.view_partner_form">
                            <field name="id" />
                            <field name="color" />
                            <field name="name" />
                            <field name="title" />
                            <field name="type" />
                            <field name="email" />
                            <field name="practice_id" />
                            <field name="is_location" />
                            <field name="function" />
                            <field name="phone" />
                            <field name="street" />
                            <field name="street2" />
                            <field name="zip" />
                            <field name="city" />
                            <field name="country_id" />
                            <field name="mobile" />
                            <field name="state_id" />
                            <field name="image_128" />
                            <field name="lang" />
                            <field name="comment" />
                            <field name="display_name" />
                            <templates>
                                <t t-name="kanban-box">
                                    <t t-set="color" t-value="kanban_color(record.color.raw_value)" />
                                    <div t-att-class="color + (record.title.raw_value == 1 ? ' oe_kanban_color_alert' : '') + ' oe_kanban_global_click'">
                                        <div class="o_kanban_image">
                                            <img alt="Contact image" t-if="record.image_128.raw_value" t-att-src="kanban_image('res.partner', 'image_128', record.id.raw_value)" />
                                            <t t-if="!record.image_128.raw_value">
                                                <img alt="Delivery" t-if="record.type.raw_value === 'delivery'" t-att-src='_s + "/base/static/img/truck.png"'/>
                                                <img alt="Invoice" t-if="record.type.raw_value === 'invoice'" t-att-src='_s + "/base/static/img/money.png"'/>
                                                <t t-if="record.type.raw_value !== 'invoice' &amp;&amp; record.type.raw_value !== 'delivery'">
                                                    <img alt="Logo" t-if="record.is_location.raw_value === true" t-att-src='_s + "/base/static/img/company_image.png"'/>
                                                    <img alt="Avatar" t-if="record.is_location.raw_value === false" t-att-src='_s + "/base/static/img/avatar_grey.png"'/>
                                                </t>
                                            </t>
                                        </div>
                                        <div class="oe_kanban_details">
                                            <field name="name" />
                                            <div t-if="record.function.raw_value">
                                                <field name="function" />
                                            </div>
                                            <div t-if="record.email.raw_value">
                                                <field name="email" widget="email" />
                                            </div>
                                            <div t-if="record.type.raw_value != 'contact'">
                                                <div>
                                                    <field name="zip" />
                                                    <field name="city" />
                                                </div>
                                                <field t-if="record.state_id.raw_value" name="state_id" />
                                                <field name="country_id" />
                                            </div>
                                            <div t-if="record.phone.raw_value">Phone: <t t-esc="record.phone.value" />
                                            </div>
                                            <div t-if="record.mobile.raw_value">Mobile: <t t-esc="record.mobile.value" />
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                    </field>
                </page>
            </xpath> -->
        </field>
    </record>

    <record id="pod_practice_action" model="ir.actions.act_window">
        <field name="name">Podiatry Practices</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="domain">[('is_practice', '=', True)]</field>
        <field name="context">{
            'default_company_type':'company',
            'default_is_company':True, 
            'default_is_practice': True,
            'default_is_pod': True}
        </field>
    </record>

</odoo>
