<odoo>

  <!-- Podiatry App Menu -->
  <menuitem id="menu_pod" name="Podiatry" />

  <!-- Action to open the Item list -->
  <record id="action_pod_item" model="ir.actions.act_window">
    <field name="name">Podiatry Items</field>
    <field name="res_model">pod.item</field>
    <field name="view_mode">tree,form</field>
  </record>
  <!-- Old act_wndow, deprecated
  <act_window id="action_pod_item" name="Podiatry Items" res_model="pod.item" view_mode="tree,form" />
  -->
  <!-- Menu item to open the Item list -->
  <!-- <menuitem id="menu_pod_item" name="Items" parent="menu_pod" action="action_pod_item" /> -->

  <menuitem id="menu_pod_configuration" name="Configuration" parent="menu_pod" sequence="100"/>

  <menuitem id="menu_pod_item" name="Items" parent="menu_pod_configuration" action="action_pod_item" sequence="20"/>



  <!-- <menuitem id="menu_pod_stage" name="Stages" action="action_pod_stage" parent="pod_app.menu_pod_configuration" /> -->



</odoo>
