<odoo>
  <record id="view_form_prescription" model="ir.ui.view">
    <field name="name">Prescription Form</field>
    <field name="model">pod.prescription</field>
    <field name="arch" type="xml">

      <form>
        <!-- <header>
          <button name="button_check_isbn" type="object" string="Check ISBN" />
        </header> -->
        <sheet>
          <group name="group_top">
            <group name="group_left">
              <field name="name" />
              <field name="doctor_ids" widget="many2many_tags" />
              <field name="prescriber_id" />
              <field name="date_prescribed" />
            </group>
            <group name="group_right">
              <field name="isbn" />
              <field name="active" />
              <field name="image" widget="image" />
            </group>
          </group>
        </sheet>
      </form>

    </field>
  </record>

  <record id="view_tree_prescription" model="ir.ui.view">
    <field name="name">Prescription List</field>
    <field name="model">pod.prescription</field>
    <field name="arch" type="xml">
      <tree>
        <field name="name"/>
        <field name="doctor_ids" widget="many2many_tags" />
        <field name="prescriber_id"/>
        <field name="date_prescribed"/>
      </tree>
    </field>
  </record>

  <record id="view_search_prescription" model="ir.ui.view">
    <field name="name">Prescription Filters</field>
    <field name="model">pod.prescription</field>
    <field name="arch" type="xml">
      <search>
        <field name="prescriber_id"/>
        <filter name="filter_inactive" string="Inactive" domain="[('active','=',True)]"/>
        <filter name="filter_active" string="Active" domain="[('active','=',False)]"/>
      </search>
    </field>
  </record>

</odoo>
