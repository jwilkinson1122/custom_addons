<odoo>
  <record id="view_form_patient" model="ir.ui.view">
    <field name="name">Clinic Patient Form View</field>
    <field name="model">clinic.patient</field>
    <field name="arch" type="xml">

      <form>
        <group>
          <field name="name" />
          <field name="email" />
          <field name="patient_number" />
        </group>
        <!-- mail mixin fields -->
        <div class="oe_chatter">
          <field name="message_follower_ids" widget="mail_followers"/>
          <field name="activity_ids" widget="mail_activity"/>
          <field name="message_ids" widget="mail_thread"/>
        </div>

      </form>
    </field>
  </record>

  <record id="view_tree_patient" model="ir.ui.view">
    <field name="name">Clinic Patient List View</field>
    <field name="model">clinic.patient</field>
    <field name="arch" type="xml">
      <tree>
        <field name="name" />
        <field name="patient_number" />
      </tree>
    </field>
  </record>
</odoo>
