<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="pod_encounter_view_form" model="ir.ui.view">
        <field name="name">pod.encounter.view.form</field>
        <field name="model">pod.encounter</field>
        <field
            name="inherit_id"
            ref="nwp_pod_careplan_sale.pod_encounter_view_form"
        />
        <field name="arch" type="xml">
            <button name="create_sale_order" position="after">
                <button
                    name="recompute_commissions"
                    type="object"
                    string="Recompute commissions"
                    groups="pod_base.group_pod_financial"
                    class="oe_highlight"
                />
            </button>
        </field>
    </record>
</odoo>
