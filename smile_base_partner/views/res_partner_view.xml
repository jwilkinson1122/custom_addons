<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='parent_id']" position="after">
                <field name="can_have_parent" invisible="1"/>
                <field name="parent_is_required" invisible="1"/>
                <field name="parent_type_ids" invisible="1"/>
                <field name="partner_type_code" invisible="1"/> 
            </xpath>
            <xpath expr="//field[@name='parent_id']" position="before">
                <field name="customer" invisible="1"/>
                <field name="supplier" invisible="1"/>
                <field name="parent_type_ids" invisible="1"/>
                <field name="partner_type_code" invisible="1"/> 
                <field name="partner_type_id" class="oe_inline" widget="selection" domain="[('company_type', '=', company_type)]"/>
                <field name="parent_relation_label" class="oe_inline" nolabel="1" invisible="not can_have_parent or not partner_type_id"/>
            </xpath>
            <xpath expr="//field[@name='parent_id']" position="attributes">
                <attribute name="invisible">not can_have_parent or not partner_type_id</attribute>
                <attribute name="required">parent_is_required</attribute>
                <attribute name="class">oe_inline</attribute>
                <attribute name="placeholder">Company</attribute>
                <attribute name="domain">[('is_company', '=', True)]</attribute>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="view_affiliates" type="object" class="oe_stat_button" icon="fa-building" invisible="affiliates_count == 0">
                    <span>
                        <field name="affiliates_count" widget="statinfo" nolabel="1"/>
                        <field name="affiliates_label"/>
                    </span>
                </button>
            </xpath>
            <xpath expr="//field[@name='type']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//page[@name='contact_addresses']" position="before">
                <page string="Affiliates" name="affiliates_page" autofocus="True">
                    <group>
                        <label for="affiliate_ids" string="Direct Affiliates" class="fw-bolder mb-3 mt-4 small text-uppercase" />
                    </group>
                    <field name="affiliate_ids" mode="kanban" nolabel="1">
                        <kanban create="true">
                            <field name="id" />
                            <field name="color" />
                            <field name="name" />
                            <field name="title" />
                            <field name="type" />
                            <field name="email" />
                            <field name="parent_id" />
                            <field name="is_company" />
                            <field name="function" />
                            <field name="phone" />
                            <field name="street" />
                            <field name="street2" />
                            <field name="zip" />
                            <field name="city" />
                            <field name="country_id" />
                            <field name="mobile" />
                            <field name="state_id" />
                            <field name="image_128" />
                            <field name="avatar_128"/>
                            <field name="lang" />
                            <field name="comment" />
                            <field name="display_name" />
                            <templates>
                                <t t-name="kanban-box">
                                    <t t-set="color" t-value="kanban_color(record.color.raw_value)" />
                                    <div t-att-class="color + (record.title.raw_value == 1 ? ' oe_kanban_color_alert' : '') + ' oe_kanban_global_click'">
                                        <div class="o_kanban_image">
                                            <!-- <img alt="Contact image" t-att-src="kanban_image('res.partner', 'image_128', record.id.raw_value or False)" /> -->

                                            <img alt="Contact image" t-att-src="kanban_image('res.partner', 'avatar_128', record.id.raw_value)" />
                                        </div>
                                        <!-- <div class="o_kanban_image">
                                                    <img alt="Contact image" t-att-src="kanban_image('res.partner', 'avatar_128', record.id.raw_value)"/>
                                                </div> -->
                                        <div class="oe_kanban_details">
                                            <field name="name" />
                                            <div t-if="record.function.raw_value">
                                                <field name="function" />
                                            </div>
                                            <div t-if="record.email.raw_value">
                                                <field name="email" widget="email" />
                                            </div>
                                            <div t-if="record.type.raw_value != 'contact'">
                                                <div>
                                                    <field name="zip" />
                                                    <field name="city" />
                                                </div>
                                                <field t-if="record.state_id.raw_value" name="state_id" />
                                                <field name="country_id" />
                                            </div>
                                            <div t-if="record.phone.raw_value">Phone: <field name="phone" />
                                            </div>
                                            <div t-if="record.mobile.raw_value">Mobile: <field name="mobile" />
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Contact / Address">
                            <sheet>
                                <field name="type" required="1" widget="radio" options="{'horizontal': true}" />
                                <field name="parent_id" invisible="1" />
                                <div class="text-muted oe_edit_only">
                                    <p class="mb-0" invisible="type != 'contact'">
                                        <span
                                        >Use this to organize the contact details of employees of a given company (e.g. CEO, CFO, ...).</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'invoice'">
                                        <span
                                        >Preferred address for all invoices. Selected by default when you invoice an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'delivery'">
                                        <span
                                        >Preferred address for all deliveries. Selected by default when you deliver an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'other'">
                                        <span
                                        >Other address for the company (e.g. subsidiary, ...)</span>
                                    </p>
                                </div>
                                <hr />
                                <group>
                                    <group>
                                        <field name="name" string="Contact Name" required="type == 'contact'" />
                                        <field name="title" options="{'no_open': True}" placeholder="e.g. Mr." invisible="type != 'contact'" />
                                        <field name="function" placeholder="e.g. Sales Director" invisible="type != 'contact'" />
                                        <label for="street" string="Address" invisible="type == 'contact'" />
                                        <div invisible="type == 'contact'">
                                            <div class="o_address_format" name="div_address">
                                                <field name="street" placeholder="Street..." class="o_address_street" />
                                                <field name="street2" placeholder="Street 2..." class="o_address_street" />
                                                <field name="city" placeholder="City" class="o_address_city" />
                                                <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}" />
                                                <field name="zip" placeholder="ZIP" class="o_address_zip" />
                                                <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}" />
                                            </div>
                                        </div>
                                    </group>
                                    <group>
                                        <field name="email" widget="email" />
                                        <field name="phone" widget="phone" />
                                        <field name="mobile" widget="phone" />
                                        <field name="company_id" invisible="1" />
                                    </group>
                                </group>
                                <group>
                                    <field name="comment" placeholder="Internal notes..." nolabel="1" colspan="2" />
                                </group>
                                <field name="lang" invisible="True" />
                                <field name="image_128" invisible="True" />
                                <field name="user_id" invisible="True" />
                            </sheet>
                        </form>
                    </field>
                    <group>
                        <label for='sub_affiliate_ids' string="Indirect Affiliates" colspan="2" class="fw-bolder mb-3 mt-4 small text-uppercase" />
                        <field name="sub_affiliate_ids" mode="tree" invisible="not sub_affiliate_ids" colspan="2" context="{'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id}" nolabel="1">
                            <tree>
                                <field name="id" />
                                <field name="name" />
                                <field name="parent_id" />
                                <field name="function" />
                                <field name="email" />
                                <field name="phone" />
                                <field name="street" />
                                <field name="street2" />
                                <field name="zip" />
                                <field name="city" />
                                <field name="state_id" />
                                <field name="country_id" />
                            </tree>
                        </field>
                    </group>
                </page>
            </xpath>
            <!-- Contacts Notebook Page -->
            <xpath expr="//page[@name='contact_addresses']" position="attributes">
                <attribute name="autofocus"></attribute>
                <attribute name="string">Contacts</attribute>
            </xpath>
            <xpath expr="//field[@name='child_ids']" position="attributes">
                <attribute name="domain">[('partner_type_code', '!=', 'PATIENT'), ('is_company', '=', False)]</attribute>
            </xpath>
            <xpath expr="//field[@name='child_ids']" position="before">
                <label for='child_ids' string="Direct Contacts" class="fw-bolder mb-3 mt-4 small text-uppercase" />
            </xpath>
            <xpath expr="//field[@name='child_ids']" position="after">
                <group>
                    <label for='sub_contact_ids' string="Indirect Contacts" colspan="2" class="fw-bolder mb-3 mt-4 small text-uppercase" />
                    <field name="sub_contact_ids" mode="tree" invisible="not sub_contact_ids" colspan="2" context="{'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id}" nolabel="1">
                        <tree>
                            <field name="id" />
                            <field name="name" />
                            <field name="parent_id" />
                            <field name="function" />
                            <field name="email" />
                            <field name="phone" />
                            <field name="street" />
                            <field name="street2" />
                            <field name="zip" />
                            <field name="city" />
                            <field name="state_id" />
                            <field name="country_id" />
                        </tree>
                    </field>
                </group>
            </xpath>
            <xpath expr="//field[@name='child_ids']/form//field[@name='type']" position="before">
                <field name="partner_type_id" required="1" domain="[('company_type', '=', 'person')]" string="Address type" widget="radio" options="{'horizontal': true}"/>
            </xpath>
            <xpath expr="//field[@name='child_ids']/form//field[@name='type']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr='//page[@name="contact_addresses"]' position="after">
                <page name="patients" string="Patients" invisible="partner_type_code == 'PATIENT'">
                    <group>
                        <label for="patient_ids" string="Direct Patients" class="fw-bolder mb-3 mt-4 small text-uppercase" />
                    </group>
                    <field name="patient_ids" mode="kanban" nolabel="1" domain="[('partner_type_code', '=', 'PATIENT')]" context="{'default_partner_type_code': 'PATIENT'}">
                        <kanban create="true">
                            <field name="id" />
                            <field name="color" />
                            <field name="name" />
                            <field name="title" />
                            <field name="partner_type_id"/>
                            <field name="partner_type_code" />
                            <field name="type" />
                            <field name="email" />
                            <field name="parent_id" />
                            <field name="is_company" />
                            <field name="function" />
                            <field name="phone" />
                            <field name="street" />
                            <field name="street2" />
                            <field name="zip" />
                            <field name="city" />
                            <field name="country_id" />
                            <field name="mobile" />
                            <field name="state_id" />
                            <field name="image_128" />
                            <field name="avatar_128"/>
                            <field name="lang" />
                            <field name="comment" />
                            <field name="display_name" />
                            <templates>
                                <t t-name="kanban-box">
                                    <t t-set="color" t-value="kanban_color(record.color.raw_value)" />
                                    <div t-att-class="color + (record.title.raw_value == 1 ? ' oe_kanban_color_alert' : '') + ' oe_kanban_global_click'">
                                        <div class="o_kanban_image">
                                            <img alt="Contact image" t-att-src="kanban_image('res.partner', 'avatar_128', record.id.raw_value)" />
                                        </div>
                                        <div class="oe_kanban_details">
                                            <field name="name" />
                                            <div t-if="record.email.raw_value">
                                                <field name="email" widget="email" />
                                            </div>
                                            <div t-if="record.partner_type_code.raw_value != 'PATIENT'">
                                                <div>
                                                    <field name="zip" />
                                                    <field name="city" />
                                                </div>
                                                <field t-if="record.state_id.raw_value" name="state_id" />
                                                <field name="country_id" />
                                            </div>
                                            <div t-if="record.phone.raw_value">Phone: <field name="phone" />
                                            </div>
                                            <div t-if="record.mobile.raw_value">Mobile: <field name="mobile" />
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Patient / Address">
                            <sheet>
                                <field name="partner_type_code" invisible="1"/>
                                <field name="partner_type_id" widget="radio" options="{'horizontal': true}" context="{'default_partner_type_code': 'PATIENT'}" invisible="partner_type_code == 'PATIENT'"/>
                                <field name="parent_id" invisible="1" />
                                <div class="text-muted oe_edit_only">
                                    <p class="mb-0" invisible="partner_type_code == 'PATIENT'">
                                        <span
                                        >Use this to organize the contact details of patients of a given company.</span>
                                    </p>
                                </div>
                                <hr invisible="partner_type_code == 'PATIENT'"/>
                                <group>
                                    <group>
                                        <field name="name" string="Patient Name" required="partner_type_code == 'PATIENT'" />
                                        <field name="title" options="{'no_open': True}" placeholder="e.g. Mr." invisible="partner_type_code != 'PATIENT'" />
                                        <label for="street" string="Address" invisible="partner_type_code == 'PATIENT'" />
                                        <div invisible="partner_type_code == 'PATIENT'">
                                            <div class="o_address_format" name="div_address">
                                                <field name="street" placeholder="Street..." class="o_address_street" />
                                                <field name="street2" placeholder="Street 2..." class="o_address_street" />
                                                <field name="city" placeholder="City" class="o_address_city" />
                                                <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}" />
                                                <field name="zip" placeholder="ZIP" class="o_address_zip" />
                                                <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}" />
                                            </div>
                                        </div>
                                    </group>
                                    <group>
                                        <field name="email" widget="email" />
                                        <field name="phone" widget="phone" />
                                        <field name="mobile" widget="phone" />
                                        <field name="company_id" invisible="1" />
                                    </group>
                                </group>
                                <group>
                                    <field name="comment" placeholder="Internal notes..." nolabel="1" colspan="2" />
                                </group>
                                <field name="lang" invisible="True" />
                                <field name="image_128" invisible="True" />
                                <field name="user_id" invisible="True" />
                            </sheet>
                        </form>
                    </field>
                    <group>
                        <label for="sub_patient_ids" string="Indirect Patients" class="fw-bolder mb-3 mt-4 small text-uppercase" />
                        <field name="sub_patient_ids" mode="tree" invisible="not sub_patient_ids" colspan="2" domain="[('partner_type_code', '!=', 'PATIENT'), ('is_company', '=', False)]" context="{'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id}" nolabel="1">
                            <tree>
                                <field name="id" />
                                <field name="name" />
                                <field name="parent_id" />
                                <field name="email" />
                                <field name="phone" />
                            </tree>
                        </field>
                        <!-- <field name="sub_patient_ids" mode="kanban" invisible="not sub_patient_ids" colspan="2" domain="[('partner_type_code', '!=', 'PATIENT'), ('is_company', '=', False)]" context="{'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id}" nolabel="1">
                            <kanban create="false">
                                <field name="id" />
                                <field name="name" />
                                <field name="email" />
                                <field name="phone" />
                                <templates>
                                    <t t-name="kanban-box">
                                        <div class="o_kanban_record">
                                            <div>
                                                <strong>
                                                    <field name="name" />
                                                </strong>
                                            </div>
                                            <div t-if="record.email.raw_value">
                                                <small>Email: <field name="email" />
                                                </small>
                                            </div>
                                            <div t-if="record.phone.raw_value">
                                                <small>Phone: <field name="phone" />
                                                </small>
                                            </div>
                                        </div>
                                    </t>
                                </templates>
                            </kanban>
                        </field> -->
                    </group>
                </page>
            </xpath>


        </field>
    </record>

    <record id="view_partner_address_form" model="ir.ui.view">
        <field name="name">res.partner.form.address</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_address_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='type']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='type']" position="before">
                <field name="partner_type_id" required="1" domain="[('company_type', '=', 'person')]" class="oe_inline"/>
            </xpath>
        </field>
    </record>

    <record id="view_res_partner_filter" model="ir.ui.view">
        <field name="name">res.partner.select</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <filter name="type_person" position="after">
                <filter name="customer" string="Customer" domain="[('customer','=',1), '|', ('is_company', '=', True), ('parent_id', '=', False)]" help="Customers"/>
                <filter name="supplier" string="Vendor" domain="[('supplier','=',1), '|', ('is_company', '=', True), ('parent_id', '=', False)]" help="Suppliers"/>
                <filter name="patient_filter" string="Patients" domain="[('partner_type_code', '=', 'PATIENT')]" help="Patients"/>

            </filter>
        </field>
    </record>

    <record id="view_res_partner_search" model="ir.ui.view">
        <field name="name">res.partner.search</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <filter name="customer" position="attributes">
                <attribute name="domain">[('customer', '=', True), '|', ('parent_id', '=', False), ('is_company', '=',
                    True)]
                </attribute>
            </filter>
            <filter name="supplier" position="attributes">
                <attribute name="domain">[('supplier', '=', True), '|', ('parent_id', '=', False), ('is_company', '=',
                    True)]
                </attribute>
            </filter>
        </field>
    </record>

</odoo>
