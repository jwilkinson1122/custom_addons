<odoo>
  <record id="view_form_item_extend" model="ir.ui.view">
    <field name="name">Item: add Is Available? field</field>
    <field name="model">pod.item</field>
    <field name="inherit_id" ref="pod_app.view_form_item"/>
    <field name="arch" type="xml">

      <field name="isbn" position="after">
        <field name="is_available" />
      </field>
      <!-- Or, using xpath:
      <xpath expr="//field[@name='isbn']" position="after">
        <field name="is_available" />
      </xpath>
      -->

    </field>
  </record>
</odoo>
