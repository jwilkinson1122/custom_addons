<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="pod_encounter_view_form" model="ir.ui.view">
        <field name="name">pod.encounter.view.form</field>
        <field name="model">pod.encounter</field>
        <field name="inherit_id" ref="pod_administration_encounter.pod_encounter_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button type="object" name="action_view_careplans" class="oe_stat_button" icon="fa-hospital-o" attrs="{'invisible': [('state','=','planned')]}">
                    <field name="careplan_count" widget="statinfo" string="Careplans" />
                    <field name="careplan_ids" invisible="1" />
                </button>
            </xpath>
        </field>
    </record>
</odoo>
