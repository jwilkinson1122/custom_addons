<odoo>
    <data noupdate="1">
        <record model="ir.rule" id="see_his_own_patient">
            <field name="name">Patients can only access their own records</field>
            <field name="model_id" ref="model_res_partner"/>
            <field name="domain_force">[('patient_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('group_portal_patient'))]"/>
        </record>
    </data>
</odoo>
