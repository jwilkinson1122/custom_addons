<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_report_pod_quote" model="ir.actions.report">
        <field name="name">Quote</field>
        <field name="model">pod.quote</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">pod_quote.report_pod_quote</field>
        <field name="report_file">pod_quote.report_pod_quote</field>
        <field name="print_report_name">'Presupuesto - %s' % (object.name)</field>
    </record>

    <record id="action_report_pod_quote_patient" model="ir.actions.report">
        <field name="name">Patient copy</field>
        <field name="model">pod.quote</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">pod_quote.report_pod_quote_patient</field>
        <field name="report_file">pod_quote.report_pod_quote_patient</field>
        <field name="print_report_name">'Actividad - %s' % (object.name)</field>
    </record>
</odoo>
