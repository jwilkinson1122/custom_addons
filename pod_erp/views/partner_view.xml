<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form -->
    <record id="view_res_partner_form_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- <xpath expr="//field[@name='company_type']" position="after">
                <group colspan="4" col="4">
                    <field name="is_patient" />
                    <field name="is_practitioner" />
                    <field name="is_practice" />
                </group>
            </xpath> -->
            <notebook>
                <page string="Medical">
                    <group colspan="6" col="6">
                        <field name="is_patient" />
                        <field name="is_doctor" />
                        <field name="is_practice" />
                    </group>
                    <!-- <field name="doctor_ids" colspan="4" widget="one2many_list" /> -->
                    <!-- <field name="patient_ids" colspan="4" widget="one2many_list" /> -->
                </page>
            </notebook>
        </field>
    </record>


    <record id="partners_action" model="ir.actions.act_window">
        <field name="name">Partner</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Partner
            </p>
        </field>
    </record>







</odoo>