<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record model="ir.ui.view" id="file_storage_form_view">
        <field name="name">file.storage.form (in file_attachment)</field>
        <field name="model">file.storage</field>
        <field name="inherit_id" ref="pod_file_storage.file_storage_form_view" />
        <field name="arch" type="xml">
            <field name="options" position="after">
                <field name="model_ids" widget="many2many">
                    <tree>
                        <field name="name" />
                        <field name="model" />
                    </tree>
                </field>
                <field name="field_ids" widget="many2many">
                    <tree>
                        <field name="name" />
                        <field name="model" />
                    </tree>
                </field>
                <separator string="Attachment" />
                <field name="optimizes_directory_path" />
                <field name="autovacuum_gc" />
                <field name="use_as_default_for_attachments" />
                <field name="force_db_for_default_attachment_rules" widget="ace" options="{'mode': 'python'}" invisible="not use_as_default_for_attachments"/>
                <separator string="Attachment's Url" />
                <field name="base_url" />
                <field name="is_directory_path_in_url" />
                <field name="use_filename_obfuscation" />
                <field name="use_x_sendfile_to_serve_internal_url" />
            </field>
        </field>
    </record>

</odoo>
