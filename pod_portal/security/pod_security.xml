<odoo>
  <data noupdate="1">
    <record id="member_portal_rule" model="ir.rule">
      <field name="name">Podiatry Member Portal Access</field>
      <field name="model_id" ref="pod_member.model_pod_member"/>
      <field name="domain_force">[('partner_id', '=', user.partner_id.id)]
      </field>
      <field name="groups" eval="[(4,ref('base.group_portal'))]"/>
    </record>

    <record id="prescription_portal_rule" model="ir.rule">
      <field name="name">Podiatry Prescription Portal Access</field>
      <field name="model_id" ref="pod_prescription.model_pod_prescription"/>
      <field name="domain_force">[('member_id.partner_id', '=', user.partner_id.id)]</field>
      <field name="groups" eval="[(4,ref('base.group_portal'))]"/>
    </record>
  </data>
</odoo>
