<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
    <record id="room_booking_detail_view_form" model="ir.ui.view">
        <field name="name">room.booking.detail.view.form</field>
        <field name="model">room.booking.detail</field>
        <field name="arch" type="xml">
            <form name="report">
                <group col="4">
                    <field name="checkin"/>
                    <field name="checkout"/>
                    <field name="room_id" option="{'no_create': True, 'no_quick_create': True, 'no_create_edit': True}"/>
                </group>
                <footer>
                    <button name="action_room_booking_pdf" string="PDF" type="object" class="btn-primary">
                        <icon name="fa fa-download mr-2"/>
                        Download Pdf
                    </button>
                    <button name="action_room_booking_excel" type="object"
                            class="btn-primary">
                        <i class="fa fa-download mr-2"/>
                        Download XLS
                    </button>
                    <button special="cancel" string="Cancel"
                            class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

<!--    room booking report menu action-->
    <record id="room_booking_detail_action" model="ir.actions.act_window">
        <field name="name">Room Booking Detail</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">room.booking.detail</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="room_booking_detail_view_form"/>
        <field name="target">new</field>
    </record>
</odoo>