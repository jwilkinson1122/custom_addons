<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">payment.provider.form.inherit.prescriptions</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="payment_form" position="inside">
                <field name="rx_reference_type" invisible="code != 'custom'"/>
            </group>
        </field>
    </record>

    <record id="transaction_form_inherit_prescription" model="ir.ui.view">
        <field name="name">payment.transaction.form.inherit.prescriptions.payment</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button name="action_view_prescriptions_orders" type="object"
                        class="oe_stat_button" icon="fa-money"
                        invisible="prescriptions_order_ids_nbr == 0">
                    <field name="prescriptions_order_ids_nbr" widget="statinfo" string="Prescriptions Order(s)"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
