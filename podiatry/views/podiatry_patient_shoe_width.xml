<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
    Form
    -->
    <record id="view_podiatry_patient_shoe_width_form" model="ir.ui.view">
        <field name="name">podiatry.patient.shoe_width.form</field>
        <field name="model">podiatry.patient.shoe_width</field>
        <field name="arch" type="xml">
            <form string="Shoe Width">
                <field name="active" invisible="1"/>
                <sheet>
                    <!--
                    Button Box
                    -->
                    <div name="button_box" class="oe_button_box"/>

                    <!--
                    Ribbons
                    -->
                    <widget name="web_ribbon" text="Archived" bg_color="bg-danger" attrs="{'invisible': [('active', '=', True)]}"/>

                    <!--
                    Title
                    -->
                    <div class="oe_title">
                        <h1 name="name">
                            <field name="name" placeholder="e.g. Shoe Width"/>
                        </h1>
                    </div>

                    <!--
                    Content
                    -->
                    <group>
                        <group>
                            <field name="code"/>
                        </group>
                    </group>

                    <!--
                    Notebook
                    -->
                    <notebook>
                        <page name="notes" string="Notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!--
    Tree
    -->
    <record id="view_podiatry_patient_shoe_width_tree" model="ir.ui.view">
        <field name="name">podiatry.patient.shoe_width.tree</field>
        <field name="model">podiatry.patient.shoe_width</field>
        <field name="arch" type="xml">
            <tree string="Shoe Widths" editable="top" decoration-muted="not active">
                <field name="sequence" widget="handle"/>
                <field name="name" decoration-bf="1"/>
                <field name="code"/>
                <field name="notes" optional="hide"/>
                <field name="active" invisible="1"/>
            </tree>
        </field>
    </record>

    <!--
    Search
    -->
    <record id="view_podiatry_patient_shoe_width_search" model="ir.ui.view">
        <field name="name">podiatry.patient.shoe_width.search</field>
        <field name="model">podiatry.patient.shoe_width</field>
        <field name="arch" type="xml">
            <search string="Shoe Widths">
                <field name="name" filter_domain="['|', ('name', 'ilike', self), ('code', 'ilike', self)]"/>
                <field name="code" operator="="/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <!--
    Action
    -->
    <record id="action_podiatry_patient_shoe_width" model="ir.actions.act_window">
        <field name="name">Shoe Widths</field>
        <field name="res_model">podiatry.patient.shoe_width</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_podiatry_patient_shoe_width_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new shoe width
            </p>
            <p>
                Organize and manage shoe widths at a glance.
            </p>
        </field>
    </record>

    <!--
    Submenu: Settings / Shoe Widths
    -->

    <menuitem id="menu_podiatry_patient_shoe_width" name="Shoe Widths" parent="menu_configuration_patient" action="action_podiatry_patient_shoe_width" groups="group_manager" sequence="10" />

</odoo>
