<odoo>
    <record id="view_partner_form_inherit_patient" model="ir.ui.view">
        <field name="name">res.partner.form.patient</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Add a checkbox to indicate if this is a patient -->
            <xpath expr="//group[@name='contact_info']" position="inside">
                <field name="patient"/>
            </xpath>

            <!-- Add a new tab for Patient Data -->
            <xpath expr="//page[@name='contact_addresses']" position="after">
                <page string="Patient Data" attrs="{'invisible': [('patient', '=', False)]}">
                    <field name="patient_ids" mode="tree,form" context="{'default_partner_id': active_id}">
                        <tree string="Prescriptions">
                            <field name="prescription_date"/>
                            <field name="practitioner"/>
                            <field name="material"/>
                            <field name="special_notes"/>
                        </tree>
                        <form string="Patient Prescription">
                            <group>
                                <field name="prescription_date"/>
                                <field name="practitioner"/>
                                <field name="material"/>
                                <field name="special_notes"/>
                                <field name="left_foot_details"/>
                                <field name="right_foot_details"/>
                                <field name="attachment"/>
                            </group>
                        </form>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
