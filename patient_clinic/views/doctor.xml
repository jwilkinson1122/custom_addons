<odoo>
  <data>

    <!-- Doctor Tree View -->
    <record model="ir.ui.view" id="patient_clinic_doctor_tree">
      <field name="name">patient_clinic.doctor_tree</field>
      <field name="model">patient_clinic.doctor</field>
      <field name="arch" type="xml">
        <tree>
          <field name="doctor_id" />
          <field name="name" />
          <field name="phone" />
          <field name="email" />
        </tree>
      </field>
    </record>

    <!-- Doctor Form View -->
    <record model="ir.ui.view" id="patient_clinic_doctor_form">
      <field name="name">patient_clinic.doctor_form</field>
      <field name="model">patient_clinic.doctor</field>
      <field name="arch" type="xml">
        <form string="Doctor">
          <header>
            <button name="%(patient_clinic.create_patient_wizard)d" string="Create Patient" type="action" class="oe_highlight" context="{'default_owner': active_id}" />
          </header>
          <sheet>
            <!-- Doctor Button Box -->
            <div class="oe_button_box" name="button_box">
              <button name="open_doctor_patient" class="oe_stat_button" type="object" icon="fa-address-book-o">
                <field string="Patients" name="patient_count" widget="statinfo" />
              </button>
              <button name="open_doctor_prescription" class="oe_stat_button" type="object" icon="fa-file-text-o">
                <field string="Prescriptions" name="prescription_count" widget="statinfo" />
              </button>
            </div>
            <div class="oe_title">
              <h1>
                <field name="doctor_id" />
              </h1>
            </div>
            <group>
              <field name="image" widget="image" class="oe_avatar" />
            </group>
            <group>
              <group>
                <field name="name" />
                <field name="phone" />

                <!-- <field name="age" /> -->
                <!-- <field name="gender" /> -->
              </group>
              <group>
                <!-- <field name="phone" /> -->
                <field name="email" />
                <field name="address" />
              </group>
            </group>
            <group>
              <field name="patient" widget="many2many_kanban" domain="[('owner', '=', False)]" />
            </group>
            <group name="Speciality">
              <field name="item_service" />
            </group>
          </sheet>
          <div class="oe_chatter">
            <field name="message_follower_ids" widget="mail_followers" />
            <field name="activity_ids" widget="mail_activity" />
            <field name="message_ids" widget="mail_thread" options="{'post_refresh': 'recipients'}" />
          </div>
        </form>
      </field>
    </record>

    <!-- Doctor Kanban View -->
    <record id="patient_clinic_doctor_kanban" model="ir.ui.view">
      <field name="name">patient_clinic.doctor_kanban</field>
      <field name="model">patient_clinic.doctor</field>
      <field name="arch" type="xml">
        <kanban>
          <field name="id" />
          <field name="image" />
          <field name="name" />
          <field name="phone" />
          <field name="email" />
          <field name="address" />
          <templates>
            <t t-name="kanban-box">
              <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_patient_clinic_patient_kanban">
                <t t-if="record.image.raw_value">
                  <div class="o_kanban_image_fill_left d-none d-md-block" t-attf-style="background-image:url('#{kanban_image('patient_clinic.doctor', 'image', record.id.raw_value)}')" />
                </t>
                <t t-else="">
                  <img class="o_kanban_image" alt="Avatar" t-att-src="_s + '/base/static/img/avatar.png'" />
                </t>
                <div class="oe_kanban_details">
                  <strong class="o_kanban_record_title">
                    <field name="name" />
                  </strong>
                  <div class="o_kanban_tags_section" />
                  <ul>
                    <li>
                      Phone :
                      <field name="phone" />
                    </li>
                    <li>
                      Email :
                      <field name="email" />
                    </li>
                    <li>
                      Address :
                      <field name="address" />
                    </li>
                  </ul>
                </div>
              </div>
            </t>
          </templates>
        </kanban>
      </field>
    </record>

    <!-- Doctor Search View -->
    <record id="patient_clinic_doctor_search" model="ir.ui.view">
      <field name="name">patient_clinic.doctor_search</field>
      <field name="model">patient_clinic.doctor</field>
      <field name="arch" type="xml">
        <search string="Search Doctor">
          <field name="name" filter_domain="['|',('name', 'ilike', self),('doctor_id', 'ilike', self)]" />
          <field name="item_service" />
          <field name="doctor_id" />
          <field name="name" />
          <separator />
          <group expand="0" string="Group By">
            <filter string="Speciality" name="item_service" context="{'group_by':'item_service'}" />
          </group>

        </search>
      </field>
    </record>

    <!-- Doctor Action Window -->
    <record model="ir.actions.act_window" id="doctor_clinic_doctor_action_window">
      <field name="name">Doctor</field>
      <field name="res_model">patient_clinic.doctor</field>
      <field name="view_mode">kanban,tree,form</field>
    </record>

    <!-- Menu Root -->
    <menuitem name="Patient Clinic" id="patient_clinic.root_menu" web_icon="patient_clinic,static/description/icon.png" />

    <!-- Menu Patient -->
    <menuitem name="Patient" id="patient_clinic.patient" parent="patient_clinic.root_menu" />

    <!-- Menu Patient -> Doctor -->
    <menuitem name="Doctor" id="patient_clinic.menu_doctor" parent="patient_clinic.patient" action="doctor_clinic_doctor_action_window" />
  </data>
</odoo>